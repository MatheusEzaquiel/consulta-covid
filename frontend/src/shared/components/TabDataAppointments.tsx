import { Alert } from "react-bootstrap";
export const TabDataAppointments = () => {
    
    /*
    const appointmentsList: string[] = ["Sintomas insuficientes", "Potencial Infectado", "Possível Infectado"];

    const appointments: JSX.Element[] = appointmentsList.map((appointment, index) => {
        return  (
            <>
                <Alert key="primary" variant="primary">
                    <p>Data da consulta: 21/05/2023 </p>
                    <p>Resultado: {appointment}</p>
                </Alert>
            </>
            
        )
        
    })

    return (
        <div>
            {appointments}
        </div>
        
    )
    */

    return <h1>Consultas</h1>;
}